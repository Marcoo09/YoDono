/**
 * @param {any}   _item         the item which will be rendered.
 * @param {number} index        the index of the item in the list.
 */
export const keyExtractor = (_item: any, index: number) => `${index}`;
